#include <stdio.h>

int main()
{
  char str[10000],tot[10000];
  int i,km=0;
  gets(str);
  gets(tor);
  for(i=0;str[i]!='\0';i++)
  {  
          if(str[i]!=tot[i])
          {
              
             km++; 
           

          }
   
      }
  
  if(km==1)
  printf("yes");
  else
  printf("no");
    return 0;
}
