#include <stdio.h>
int main()
{
    int num,i,count=0;
	scanf("%d",&num);
    for(i=2;i<num;i++)
    {
        if(i%2==1 && n%umi==0)
        {
            printf("%d",i);
            
        }
    }
	return 0;
}
